<!-- Local rules -->
<!-- Modify it at your will. -->
<!-- Copyright (C) 2015, Wazuh Inc. -->

<group name="windows,authentication,brute_force">

  <!-- Single failed logon -->
  <rule id="100001" level="5">
    <if_group>windows</if_group>
    <field name="win.system.eventID">^4625$</field>
    <description>Windows: Failed logon attempt by user $(win.eventdata.targetUserName)</description>
    <mitre>
      <id>T1110</id>
    </mitre>
  </rule>

  <!-- Brute force - 5 failures in 2 minutes -->
  <rule id="100002" level="10" frequency="5" timeframe="120">
    <if_matched_sid>100001</if_matched_sid>
    <description>Windows: Brute Force attack detected - Multiple failed logons from $(win.eventdata.ipAddress)</description>
    <mitre>
      <id>T1110</id>
    </mitre>
  </rule>

  <!-- Port scan detection -->
  <rule id="100003" level="8">
    <if_group>windows</if_group>
    <field name="win.system.eventID">^5156$</field>
    <field name="win.eventdata.direction">%%14592</field>
    <description>Windows: Possible port scan detected from $(win.eventdata.sourceAddress)</description>
    <mitre>
      <id>T1046</id>
    </mitre>
  </rule>

</group>

<group name="windows,powershell,attack">

  <!-- Encoded PowerShell command -->
  <rule id="100004" level="10">
    <if_group>windows</if_group>
    <field name="win.system.eventID">^4104$</field>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)(encodedcommand|-enc\s)</field>
    <description>Windows: Suspicious encoded PowerShell command executed</description>
    <mitre>
      <id>T1059.001</id>
    </mitre>
  </rule>

  <!-- PowerShell download cradle -->
  <rule id="100005" level="12">
    <if_group>windows</if_group>
    <field name="win.system.eventID">^4104$</field>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)(invoke-webrequest|iwr|webclient|downloadstring|downloadfile|bitstransfer)</field>
    <description>Windows: PowerShell possible download cradle detected</description>
    <mitre>
      <id>T1059.001</id>
    </mitre>
  </rule>

  <!-- PowerShell reconnaissance -->
  <rule id="100006" level="8">
    <if_group>windows</if_group>
    <field name="win.system.eventID">^4104$</field>
    <field name="win.eventdata.scriptBlockText" type="pcre2">(?i)(whoami|net user|net localgroup|ipconfig|systeminfo|tasklist)</field>
    <description>Windows: PowerShell reconnaissance command detected</description>
    <mitre>
      <id>T1059.001</id>
    </mitre>
  </rule>

</group>
